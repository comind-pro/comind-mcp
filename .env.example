# Postgres (docker-compose). Copy to .env to override.
POSTGRES_USER=comind
POSTGRES_PASSWORD=comind
POSTGRES_DB=comind
POSTGRES_PORT=5432

# Server reads DATABASE_URL (see server/.env.example for app config)
DATABASE_URL=postgres://comind:comind@localhost:5432/comind
