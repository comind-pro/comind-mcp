{
  "name": "comind-mcp",
  "version": "0.2.0",
  "private": true,
  "description": "MCP gateway — aggregate MCP servers & APIs, compose tools, expose curated virtual MCP endpoints to agents. Self-hosted, zero-infra (SQLite).",
  "license": "MIT",
  "homepage": "https://github.com/comind-pro/comind-mcp#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/comind-pro/comind-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/comind-pro/comind-mcp/issues"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:server": "pnpm --filter comind-server dev",
    "dev:web": "pnpm --filter comind-web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "biome check server/src web/src",
    "lint:fix": "biome check --write server/src web/src",
    "format": "biome format --write server/src web/src",
    "ci": "biome ci server/src web/src",
    "db:generate": "pnpm --filter comind-server db:generate"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.1"
  }
}
